# GestureVision — Detection Application dependencies
# Install with: pip install -r requirements.txt

Flask==3.0.3
ultralytics==8.4.92
opencv-python-headless==4.10.0.84
numpy>=1.26,<2.0
torch>=2.2
torchvision>=0.17
Pillow>=10.0
PyYAML>=6.0

# Optional: only needed if you run with USE_HTTPS=1 (required for camera
# access when opening the app from a LAN IP instead of localhost)
pyopenssl>=24.0
